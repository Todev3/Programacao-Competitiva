#include <math.h>
#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>

int main(){
	long long int A, cont;
	
	scanf("%lld", &A);
	
	for(cont=0; cont < A; cont++){
		if(cont != 0)
			printf(" ");
		printf("Ho");	
	}
		
		printf("!\n");
		
	return 0;
}